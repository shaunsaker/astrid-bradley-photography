import categories from './categories';
import firebase from './firebase';
import routes from './routes';

const config = {
  categories,
  firebase,
  routes,
};

export { categories, firebase, routes };

export default config;
