import categories from './categories';
import firebase from './firebase';
import forms from './forms';
import routes from './routes';

const config = {
  categories,
  firebase,
  forms,
  routes,
};

export { categories, firebase, forms, routes };

export default config;
