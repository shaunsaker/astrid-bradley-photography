import css from 'styled-jsx/css';

const styles = css`
  .wrapper {
    height: 100vh;
    align-items: center;
  }

  .container {
    max-width: 720px;
  }

  .desktop {
    padding: 64px 24px;
  }

  .mobile {
    padding: 32px 12px;
  }
`;

export default styles;
