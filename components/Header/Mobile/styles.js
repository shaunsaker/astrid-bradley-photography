import css from 'styled-jsx/css';

const styles = css`
  .container {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    padding: 16px 24px;
    min-height: 120px;
  }

  .buttonContainer {
    align-items: flex-start;
    margin-bottom: 16px;
  }

  button {
  }
`;

export default styles;
