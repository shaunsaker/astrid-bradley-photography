import cloneObject from './cloneObject';
import getPrettyDate from './getPrettyDate';
import prepareNextAction from './prepareNextAction';

const utils = {
  cloneObject,
  getPrettyDate,
  prepareNextAction,
};

export { cloneObject, getPrettyDate, prepareNextAction };

export default utils;
