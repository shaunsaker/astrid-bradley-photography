import css from 'styled-jsx/css'; // eslint-disable-line

// FIXME: Will need to add global helper styles here
const styles = css`
  .container {
    align-items: flex-start;
    overflow-x: hidden;
  }

  .item-container {
    min-width: 100%;
  }
`;

export default styles;
