import css from 'styled-jsx/css'; // eslint-disable-line

const styles = css`
  .buttonContainer {
    position: fixed;
    bottom: 16px;
    right: 24px;
    z-index: 1;
  }

  .pageAnimator {
    position: fixed;
    bottom: 16px;
    right: 24px;
  }
`;

export default styles;
